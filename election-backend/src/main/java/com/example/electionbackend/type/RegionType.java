package com.example.electionbackend.type;

/**
 * Enum representing different types of votes.
 */
public enum RegionType {
    MUNICIPALITY, // Votes at the municipality level
    POLLING_STATION, // Votes at the polling station level
    NATIONAL, // National level votes
    CONSTITUENCY // Votes at the constituency level
}
